---
title: "Holy"
author: "Sung Inkyung"
date: '2021 10 5'
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
library(tidytext)
library(ggpattern)
library(ggforce)
library(colorspace)
library(magick)
library(ggtext)

```
###Source[tidytuesday by jake kaupp] (https://github.com/jkaupp/tidytuesdays/blob/master/2021/week25/R/analysis.R)

```{r}
bible <- read.csv("C:/Users/inkyscope/Documents/projectR/inkyscroll/data/bible_kjv_new.csv")

words <- c("holy", "sanctify", "sanctified", "consecrate", "consecrated", "consecration", "hallow")

bible_tokenized <- bible %>% 
  select(text) %>% 
  unnest_tokens(word, text) %>% 
  filter(word %in% words) %>% 
  count(word, sort = T)
  
```


```{r}
cols <- c("#e1d0be", "#fbba00", "#d41735", "#22194e")
text_cols <- c("#d41735", "#22194e", "#fbba00", "#e1d0be")


selected_words <- tibble(
  word = c("hallow", "consecrate", "sanctify", "holy"),
  count = c(15, 37, 132, 611)
) %>% 
  mutate(x0 = 0,
         y0 = c(975, 800, 550, 125),
         fill = cols,
         text_color = text_cols)

plot <- ggplot(selected_words, 
               aes(x0 = x0, y0 = y0, r = count/2)) +
  geom_circle(aes(fill = fill), 
              size = 0.1) +
  geom_text(aes(x = x0, y = y0, 
                color = text_color, 
                label = count), 
            fontface = "bold", 
            size = 5) +
  geom_text(aes(x = x0, y = y0 - count/2, 
                label = word), 
            size = 5, 
            vjust = 1.2) +
  scale_x_continuous(limits = c(-600, 600)) +
  scale_fill_identity() +
  scale_color_identity() +
  coord_equal(clip = "off") +
  labs(title = "FOUR SELECTED WORDS: HOLY, SANCTIFY, CONSECRATE, HALLOW",
       caption = "**Data**: King James Version | **Graphic**: Sung Inkyung") +
  theme_void() +
  theme(plot.background = element_rect(fill = lighten("#ede0d0", 0.7), color = NA),
        plot.title = element_text(size = 16, 
                                  hjust = 0.5),
        plot.caption = element_markdown(size = 9))

ggsave(here::here("2021", "W41_SelectedWords", "SelectedWords.png"),width = 23, height = 25, units = "cm")

  

```

